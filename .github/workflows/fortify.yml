################################################################################################################################################
# Fortify lets you build secure software fast with an appsec platform that automates testing throughout the DevSecOps pipeline. Fortify static,#
# dynamic, interactive, and runtime security testing is available on premises or as a service. To learn more about Fortify, start a free trial #
# or contact our sales team, visit microfocus.com/appsecurity.                                                                                 #
#                                                                                                                                              #
# Use this workflow template as a basis for integrating Fortify on Demand Static Application Security Testing(SAST) into your GitHub workflows.#
# This template demonstrates the steps to prepare the code+dependencies, initiate a scan, download results once complete and import into       #
# GitHub Security Code Scanning Alerts. Existing customers should review inputs and environment variables below to configure scanning against  #
# an existing application in your Fortify on Demand tenant. Additional information is available in the comments throughout the workflow, the   #
# documentation for the Fortify actions used, and the Fortify on Demand / ScanCentral Client product documentation. If you need additional     #
# assistance with configuration, feel free to create a help ticket in the Fortify on Demand portal.                                            #
################################################################################################################################################

name: Fortify on Demand Scan

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  FoD-SAST-Scan:
    # Use the appropriate runner for building your source code.
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      # Check out source code
      - name: Check Out Source Code
        uses: actions/checkout@v4

      # Java is required to run the various Fortify utilities.
      # When scanning a Java application, please use the appropriate Java version for building your application.
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: 11
          distribution: 'temurin'

      # Build the project with Maven
      - name: Build with Maven
        run: mvn -B package -DskipTests

      # Run Fortify on Demand SAST scan via the unified fortify/github-action.
      # This action bootstraps fcli, packages the code, submits the scan,
      # waits for results, and exports SARIF to GitHub Security tab.
      - name: Run Fortify Scan
        uses: fortify/github-action@v3
        env:
          FOD_URL: ${{ vars.FOD_URL }}
          FOD_CLIENT_ID: ${{ secrets.FOD_CLIENT_ID }}
          FOD_CLIENT_SECRET: ${{ secrets.FOD_CLIENT_SECRET }}
          FOD_RELEASE: JG_Github:gilseara/IWAJava           # Optional: defaults to <repo>:<branch>
          # DO_SAST_SCAN: true                 # Enabled by default
          # DO_WAIT: true                      # Wait for scan completion (default: true)
          # DO_SAST_EXPORT: true               # Export SARIF results (default: true)
          # DO_RELEASE_SUMMARY: true           # Generate release summary (default: true)
          # DO_CHECK_POLICY: false             # Run policy check (default: false)
          # DO_PR_COMMENT: false               # Add PR comment (default: false, PREVIEW)
          DO_SCA_SCAN: true
          PACKAGE_EXTRA_OPTS: "-bt mvn"
